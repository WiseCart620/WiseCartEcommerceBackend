package com.wisecartecommerce.ecommerce.enums;

public enum SectionMode {
    AUTO,       // Uses existing algorithm (featured/new/top-selling)
    MANUAL,     // Admin hand-picks specific products
    CATEGORY    // Show products from a specific category
}