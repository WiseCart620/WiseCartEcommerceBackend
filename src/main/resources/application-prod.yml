spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    clean-disabled: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME}
    password: ${EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  admin:
    email: ${ADMIN_EMAIL}
    password: ${ADMIN_PASSWORD}
  contact:
    recipient-email: ${CONTACT_RECIPIENT_EMAIL:sales@wisecart.ph}
    password: ${ADMIN_PASSWORD}
    firstName: ${ADMIN_FIRST_NAME:Admin}
    lastName: ${ADMIN_LAST_NAME:User}
  jwt:
    secret: ${JWT_SECRET}
    expiration: 86400000
    refresh-expiration: 604800000
  file:
    upload-dir: ${FILE_UPLOAD_DIR:/uploads}
    max-image-size: 5242880
    max-document-size: 10485760

server:
  port: 8080
  servlet:
    context-path: /api

flash:
  express:
    mch-id: ${FLASH_EXPRESS_MCH_ID}
    secret-key: ${FLASH_EXPRESS_SECRET_KEY}
    warehouse-no: ${FLASH_EXPRESS_WAREHOUSE_NO:}
    base-url: https://open-api.flashexpress.ph
    src-province-name: ${FLASH_EXPRESS_SRC_PROVINCE}
    src-city-name: ${FLASH_EXPRESS_SRC_CITY}
    src-postal-code: ${FLASH_EXPRESS_SRC_POSTAL}
    src-detail-address: ${FLASH_EXPRESS_SRC_DETAIL_ADDRESS}
    src-name: ${FLASH_EXPRESS_SRC_NAME}
    src-phone: ${FLASH_EXPRESS_SRC_PHONE}

maya:
  public-key: ${MAYA_PUBLIC_KEY}
  secret-key: ${MAYA_SECRET_KEY}
  base-url: https://pg.maya.ph
  success-url: https://wisecart.ph/payment/success
  failure-url: https://wisecart.ph/payment/failed
  cancel-url: https://wisecart.ph/payment/cancelled
  webhook-url: ${MAYA_WEBHOOK_URL:}